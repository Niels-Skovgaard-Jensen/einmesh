{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"einmesh","text":""},{"location":"#einops-style-multi-dimensional-meshgrids","title":"einops-style multi dimensional meshgrids","text":""},{"location":"#installation","title":"Installation","text":"<p>Simple installation from pip:</p> <pre><code>pip install einmesh\n</code></pre>"},{"location":"#api","title":"API","text":"<p>Create high dimensional meshgrids through a single command.</p> <pre><code>from einmesh import einmesh, LogSpace, LinSpace\n\nx, y = einmesh(\n    \"x y\",                                       # Name a set of axis einops style\n    x=LinSpace(start=0, end=3, num=3),           # Provide a space to sample from\n    y=LogSpace(start=0, end=1, num=3, base=10),  # -- Mix and match as you like!\n)\nprint(f\"{x=}\")\nprint(f\"{y=}\")\n</code></pre> <p>Will output</p> <pre><code>x=tensor([[0.0000, 0.0000, 0.0000],\n        [1.5000, 1.5000, 1.5000],\n        [3.0000, 3.0000, 3.0000]])\ny=tensor([[ 1.0000,  3.1623, 10.0000],\n        [ 1.0000,  3.1623, 10.0000],\n        [ 1.0000,  3.1623, 10.0000]])\n</code></pre> <p>Thus it is effectively equivalent to common torch code</p> <pre><code>x = torch.linspace(start=0, end=1, num=3)\ny = torch.logspace(start=0, end=1, num=3, base=10)\n\nx, y = torch.meshgrid(x, y, indexing=\"ij\")\n</code></pre> <p>Which in 2D is not too bad, but in high dimensions can get tireing!</p>"},{"location":"einmesh/","title":"einmesh","text":""},{"location":"einmesh/#einmesh.parser._generate_samples","title":"<code>_generate_samples(sampling_list, **kwargs)</code>","text":"<p>Generate samples from the pattern.</p> Source code in <code>src/einmesh/parser.py</code> <pre><code>def _generate_samples(sampling_list: list[str], **kwargs: SpaceType) -&gt; tuple[list[torch.Tensor], dict[str, int]]:\n    \"\"\"Generate samples from the pattern.\"\"\"\n    lin_samples: OrderedDict[str, torch.Tensor] = OrderedDict()\n    dim_shapes: dict[str, int] = {}\n    for p in sampling_list:\n        if p == \"*\":\n            continue\n        if p not in kwargs:\n            raise UndefinedSpaceError(p)\n        samples = kwargs[p]._sample()\n        lin_samples[p] = samples\n        dim_shapes[p] = samples.size()[0]\n    meshes = list(torch.meshgrid(*lin_samples.values(), indexing=\"ij\"))\n    return meshes, dim_shapes\n</code></pre>"},{"location":"einmesh/#einmesh.parser._verify_pattern","title":"<code>_verify_pattern(pattern)</code>","text":"<p>Verify the pattern is valid.</p> Source code in <code>src/einmesh/parser.py</code> <pre><code>def _verify_pattern(pattern: str) -&gt; None:\n    \"\"\"Verify the pattern is valid.\"\"\"\n    if pattern.count(\"*\") &gt; 1:\n        raise MultipleStarError()\n    if pattern.count(\"(\") != pattern.count(\")\"):\n        raise UnbalancedParenthesesError()\n    if \"_\" in pattern:\n        raise UnderscoreError()\n    if \"-&gt;\" in pattern:\n        raise ArrowError()\n</code></pre>"},{"location":"einmesh/#einmesh.parser.einmesh","title":"<code>einmesh(pattern, **kwargs)</code>","text":"<p>Einmesh is a function that takes a pattern and space objects and returns tensor(s).</p> <p>The pattern can have two forms: - Simple form: \"x y z\" - creates a mesh grid from the specified spaces. - Extended form: \"x y z -&gt; output_pattern\" - creates a mesh grid and reshapes   according to the output pattern.</p> <p>In the output pattern: - \"*\" collects all mesh tensors into a single tensor (stacking the meshes) - Parentheses like \"(y z)\" combine dimensions by reshaping using einops.rearrange</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; x = LinSpace(0, 1, 10)\n&gt;&gt;&gt; y = LinSpace(0, 1, 20)\n&gt;&gt;&gt; z = LinSpace(0, 1, 30)\n&gt;&gt;&gt; # Return a tuple of 3 meshes\n&gt;&gt;&gt; mesh_x, mesh_y, mesh_z = einmesh(\"x y z\", x=x, y=y, z=z)\n&gt;&gt;&gt; # Stack meshes into a single tensor with shape [3, 10, 20, 30]\n&gt;&gt;&gt; stacked = einmesh(\"x y z -&gt; * x y z\", x=x, y=y, z=z)\n&gt;&gt;&gt; # Reshape combining y and z dimensions\n&gt;&gt;&gt; reshaped = einmesh(\"x y z -&gt; x (y z)\", x=x, y=y, z=z)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>pattern</code> <code>str</code> <p>String pattern specifying input spaces and optional output reshaping</p> required <code>**kwargs</code> <code>SpaceType</code> <p>Space objects corresponding to the names in the pattern</p> <code>{}</code> <p>Returns:</p> Type Description <code>Tensor | tuple[Tensor, ...]</code> <p>Either a single tensor or a tuple of tensors depending on the pattern</p> Source code in <code>src/einmesh/parser.py</code> <pre><code>def einmesh(pattern: str, **kwargs: SpaceType) -&gt; torch.Tensor | tuple[\"torch.Tensor\", ...]:\n    \"\"\"\n    Einmesh is a function that takes a pattern and space objects and returns tensor(s).\n\n    The pattern can have two forms:\n    - Simple form: \"x y z\" - creates a mesh grid from the specified spaces.\n    - Extended form: \"x y z -&gt; output_pattern\" - creates a mesh grid and reshapes\n      according to the output pattern.\n\n    In the output pattern:\n    - \"*\" collects all mesh tensors into a single tensor (stacking the meshes)\n    - Parentheses like \"(y z)\" combine dimensions by reshaping using einops.rearrange\n\n    Examples:\n        &gt;&gt;&gt; x = LinSpace(0, 1, 10)\n        &gt;&gt;&gt; y = LinSpace(0, 1, 20)\n        &gt;&gt;&gt; z = LinSpace(0, 1, 30)\n        &gt;&gt;&gt; # Return a tuple of 3 meshes\n        &gt;&gt;&gt; mesh_x, mesh_y, mesh_z = einmesh(\"x y z\", x=x, y=y, z=z)\n        &gt;&gt;&gt; # Stack meshes into a single tensor with shape [3, 10, 20, 30]\n        &gt;&gt;&gt; stacked = einmesh(\"x y z -&gt; * x y z\", x=x, y=y, z=z)\n        &gt;&gt;&gt; # Reshape combining y and z dimensions\n        &gt;&gt;&gt; reshaped = einmesh(\"x y z -&gt; x (y z)\", x=x, y=y, z=z)\n\n    Args:\n        pattern: String pattern specifying input spaces and optional output reshaping\n        **kwargs: Space objects corresponding to the names in the pattern\n\n    Returns:\n        Either a single tensor or a tuple of tensors depending on the pattern\n    \"\"\"\n\n    _verify_pattern(pattern)\n\n    # get stack index\n    shape_pattern = pattern.replace(\"(\", \"\").replace(\")\", \"\")\n    stack_idx = shape_pattern.split(\" \").index(\"*\") if \"*\" in shape_pattern else None\n\n    # get sampling list\n\n    sampling_list = shape_pattern.strip().split()\n\n    meshes, dim_shapes = _generate_samples(sampling_list, **kwargs)\n\n    # Store shape information for later\n\n    # Handle star pattern for stacking meshes\n    if stack_idx is not None:\n        meshes = torch.stack(meshes, dim=stack_idx)\n\n        dim_shapes[\"einstack\"] = meshes.shape[stack_idx]\n\n    if isinstance(meshes, torch.Tensor):\n        output_pattern = pattern.replace(\"*\", \"einstack\")\n        input_pattern = pattern.replace(\"*\", \"einstack\").replace(\"(\", \"\").replace(\")\", \"\")\n        meshes = einops.rearrange(meshes, f\"{input_pattern} -&gt; {output_pattern}\", **dim_shapes)\n    if isinstance(meshes, list):\n        for i, mesh in enumerate(meshes):\n            output_pattern = pattern\n            input_pattern = pattern.replace(\"(\", \"\").replace(\")\", \"\")\n            meshes[i] = einops.rearrange(mesh, f\"{input_pattern} -&gt; {output_pattern}\", **dim_shapes)\n\n        meshes = tuple(meshes)\n\n    return meshes\n</code></pre>"},{"location":"examples/","title":"More Examples","text":"<p>More Examples comming soon!</p>"},{"location":"spaces/","title":"Spaces","text":""},{"location":"spaces/#einmesh.spaces.LatinHypercube","title":"<code>LatinHypercube</code>  <code>dataclass</code>","text":"<p>Latin Hypercube.</p> Source code in <code>src/einmesh/spaces.py</code> <pre><code>@dataclass\nclass LatinHypercube:\n    \"\"\"\n    Latin Hypercube.\n    \"\"\"\n\n    low: float\n    high: float\n    num: int\n    connector: str = \"default\"\n\n    def _sample(self) -&gt; torch.Tensor:\n        return torch.rand(self.num) * (self.high - self.low) + self.low\n</code></pre>"},{"location":"spaces/#einmesh.spaces.LinSpace","title":"<code>LinSpace</code>  <code>dataclass</code>","text":"<p>Linearly spaced points.</p> Source code in <code>src/einmesh/spaces.py</code> <pre><code>@dataclass\nclass LinSpace:\n    \"\"\"\n    Linearly spaced points.\n    \"\"\"\n\n    start: float\n    end: float\n    num: int\n\n    def _sample(self) -&gt; torch.Tensor:\n        return torch.linspace(self.start, self.end, self.num)\n</code></pre>"},{"location":"spaces/#einmesh.spaces.LogSpace","title":"<code>LogSpace</code>  <code>dataclass</code>","text":"<p>Logarithmically spaced points.</p> Source code in <code>src/einmesh/spaces.py</code> <pre><code>@dataclass\nclass LogSpace:\n    \"\"\"\n    Logarithmically spaced points.\n    \"\"\"\n\n    start: float\n    end: float\n    num: int\n    base: float = 10\n\n    def _sample(self) -&gt; torch.Tensor:\n        return torch.logspace(self.start, self.end, self.num, base=self.base)\n</code></pre>"},{"location":"spaces/#einmesh.spaces.NormalDistribution","title":"<code>NormalDistribution</code>  <code>dataclass</code>","text":"<p>Normal distribution.</p> Source code in <code>src/einmesh/spaces.py</code> <pre><code>@dataclass\nclass NormalDistribution:\n    \"\"\"\n    Normal distribution.\n    \"\"\"\n\n    mean: float\n    std: float\n    num: int\n\n    def _sample(self) -&gt; torch.Tensor:\n        return torch.normal(self.mean, self.std, size=(self.num,))\n</code></pre>"},{"location":"spaces/#einmesh.spaces.UniformDistribution","title":"<code>UniformDistribution</code>  <code>dataclass</code>","text":"<p>Uniform distribution.</p> Source code in <code>src/einmesh/spaces.py</code> <pre><code>@dataclass\nclass UniformDistribution:\n    \"\"\"\n    Uniform distribution.\n    \"\"\"\n\n    low: float\n    high: float\n    num: int\n\n    def _sample(self) -&gt; torch.Tensor:\n        return torch.rand(self.num) * (self.high - self.low) + self.low\n</code></pre>"}]}